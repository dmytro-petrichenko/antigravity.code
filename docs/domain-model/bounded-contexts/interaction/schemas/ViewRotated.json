{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ViewRotated",
    "description": "Event triggered when the user rotates the view.",
    "type": "object",
    "properties": {
        "deltaX": {
            "type": "number",
            "description": "Change in horizontal rotation (degrees or radians)"
        },
        "deltaY": {
            "type": "number",
            "description": "Change in vertical rotation (degrees or radians)"
        }
    },
    "required": [
        "deltaX",
        "deltaY"
    ]
}